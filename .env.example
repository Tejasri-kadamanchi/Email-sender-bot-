# Example .env for send_emails.py
# Copy to `.env` and fill in values, or set environment variables directly.

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your.email@gmail.com
# For Gmail, create an App Password and put it here — do NOT commit real secrets.
SMTP_PASSWORD=your_app_password_here

# Optional defaults
FROM_NAME=Your Name
EMAIL_SUBJECT=Monthly Report
